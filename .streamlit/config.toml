# Default theme for everyone (local `streamlit run` and Cloud deploy).
[theme]
base = "dark"
primaryColor = "#38bdf8"
backgroundColor = "#0E1117"
secondaryBackgroundColor = "#262730"
textColor = "#FAFAFA"
